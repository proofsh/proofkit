# notify-intent.yml — Drop this into your library repo's .github/workflows/
#
# Fires a repository_dispatch event whenever docs or source files change
# on merge to main. This triggers the skill staleness check workflow.
#
# Requirements:
#   - A fine-grained PAT with contents:write on this repository stored
#     as the INTENT_NOTIFY_TOKEN repository secret.
#
# Template variables (replaced by `intent setup`):
#   with-changesets — e.g. @tanstack/query or my-workspace workspace
#   docs/**       — e.g. docs/**
#   packages/with-changesets/src/**        — e.g. packages/query-core/src/**

name: Trigger Skill Review

on:
  push:
    branches: [main]
    paths:
      - 'packages/better-auth/docs/**'
      - 'packages/better-auth/src/**'
      - 'packages/fmdapi/docs/**'
      - 'packages/fmdapi/src/**'
      - 'packages/fmodata/docs/**'
      - 'packages/fmodata/src/**'
      - 'packages/typegen/docs/**'
      - 'packages/typegen/src/**'
      - 'packages/webviewer/docs/**'
      - 'packages/webviewer/src/**'

permissions:
  contents: write

jobs:
  notify:
    name: Trigger Skill Review
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Collect changed files
        id: changes
        run: |
          FILES=$(git diff --name-only HEAD~1 HEAD | jq -R -s -c 'split("\n") | map(select(length > 0))')
          echo "files=$FILES" >> "$GITHUB_OUTPUT"

      - name: Dispatch to intent repo
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          event-type: skill-check
          client-payload: |
            {
              "package": "with-changesets",
              "sha": "${{ github.sha }}",
              "changed_files": ${{ steps.changes.outputs.files }}
            }
