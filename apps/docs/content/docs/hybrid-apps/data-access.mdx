---
title: Data Access
description: How ProofKit WebViewer apps read and write FileMaker data.
---

import { Callout } from "fumadocs-ui/components/callout";

For hybrid apps, the primary data path is FileMaker's Execute Data API script step, called through FileMaker scripts from the WebViewer app.

## The basic flow

1. The React app needs data for a screen.
2. TanStack Query calls a ProofKit data function.
3. The WebViewer bridge calls a FileMaker script.
4. The script runs Execute Data API against a layout.
5. FileMaker returns JSON to the WebViewer.
6. The UI renders and caches the result.

## Layouts define the data shape

Execute Data API reads from FileMaker layouts, so the fields available to the web app are the fields available through those layouts.

Good layout design makes better WebViewer apps:

- Use focused layouts for single entities like companies, contacts, or invoices.
- Use composite layouts when a screen needs one primary record plus related portal data.
- Keep heavy calculated or unstored fields out of broad list queries when possible.

<Callout title="TODO: Screenshot - API layout design">
  Capture a FileMaker layout prepared for WebViewer data access, with the fields needed for a generated screen.
</Callout>

## Type safety

TypeGen reads FileMaker metadata and generates TypeScript types and validators. That gives the agent field names, record shapes, and compile-time feedback while it builds the app.

Use [TypeGen](/docs/typegen) when FileMaker layouts or fields change.

## Writes and business logic

Simple writes can follow the generated data path. More complex changes should go through custom FileMaker scripts, especially when you need:

- Transactions.
- Validation before destructive actions.
- Multi-step workflows.
- Operations that should use FileMaker's normal locking while the script runs.

Read [FileMaker Scripts as Backend](/docs/hybrid-apps/filemaker-scripts-as-backend) for that pattern.

## Full web apps

For full browser apps hosted outside FileMaker, use server-oriented transports such as the FileMaker Data API or OData. See [FMDAPI](/docs/fmdapi) and [FMOData](/docs/fmodata) for package details.
